def[168] text;
def[8] choice;
def[8] temp;

text = "Y or N [default: Y]?\n";
.output << text;

.input >> choice;

temp = (choice) == ("Y");
if (temp) {
    text = "\nYes!\n";
    .output << text;
};

temp = (choice) != ("Y");
if (temp) {
    text = "\nNo!\n";
    .output << text;
};
